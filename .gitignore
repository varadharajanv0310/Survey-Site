node_modules/
dist/
build/
.next/
out/
coverage/

.env
.env.local
.env.*.local
!.env.example

*.log
npm-debug.log*
.DS_Store
Thumbs.db

.turbo/
.drizzle/
*.tsbuildinfo

# Agent skills are vendored by `npx skills add` into three places at once
# (.agents holds the files, .claude and agent/ are junctions to it). That is
# ~9.5MB of third-party markdown tripled, and none of it is our product.
#
# skills-lock.json IS tracked — it pins the exact versions, so anyone can
# restore the same set with:
#   npx skills experimental_install
.agents/
.claude/
agent/
